.main-bg-color
  .senjin
    .senjin-top 楽天市場アイテム
    .senjin-title
      表示しているアイテムは実用的なアイテムが揃っている
      %br
      "戦人"のみのアイテムを表示しています
    .search-box
      = form_with url: search_rakutens_path, method: :get, local: true do |f|
        .text-center.py-3
          = f.text_field :keyword
          = f.submit '検索'
      .rakuten-items
        - @items.each do |item|
          .rakuten-item.mb-3
            .api-img
              = link_to item['item_url'] do
                = image_tag item['mediumImageUrls'][0]
            .api-content
              = item['itemName']
              .api
                .api-title
                  購入金額
                .api-value
                  #{item['itemPrice']}円
              .api
                .api-title 
                  平均評価
                .api-value
                  #{item['reviewAverage']}
    count: #{@total_count}
    .pagination
      #{paginate @dummy, total_pages: @total_pages}